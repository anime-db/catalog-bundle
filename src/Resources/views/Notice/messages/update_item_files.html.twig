{% set name %}
	<a href="{{ path('item_show', {'id': item.id, 'name': item.urlName}) }}">{{ item.name }}</a>
{% endset %}
{% trans with {'%name%': name} %}Changes are detected in files of item %name%{% endtrans %}
