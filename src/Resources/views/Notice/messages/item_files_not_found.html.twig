{% set name %}
    <a href="{{ path('item_show', {'id': item.id, 'name': item.urlName}) }}">{{ item.name }}</a>
{% endset %}
{% trans with {'%name%': name} %}Files for item %name% is not found{% endtrans %}
